$('#products').append('<%= j render(@products) %>');
<% if @products.total_pages == @products.current_page %>
  $("#show_more_link.hide();
<% else %>
  $("#show_more_link").attr("href", "<%= products_path(:page =>(@products.current_page+1)) %>");
<% end %>
